spring.application.name=ne-starter-template

# PostgreSQL for exam (see application-prod.properties)
# Password: src/main/resources/application-local.properties (copy from .example)
spring.profiles.active=prod
spring.config.import=optional:classpath:application-local.properties

# H2 for quick practice without Postgres: spring.profiles.active=dev

# JWT (change secret on exam day)
app.jwt.secret=UtilityBillingSystemExamSecretKey2026ChangeMeLongRandom
app.jwt.expiration-ms=86400000

# OTP
app.otp.length=6
app.otp.expiry-minutes=10

# Mail (Gmail — app password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mugishagemimah@gmail.com
spring.mail.password=zxxvqdtkllerdsnl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=mugishagemimah@gmail.com

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
# Show sections (tags) in numbered task order, and operations by HTTP method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.operations-sorter=method

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
info.app.name=${spring.application.name}
info.app.description=National exam Spring Boot ERP backend

# Logging
logging.level.com.gemimah.nestartertemplate=DEBUG
logging.level.org.springframework.security=INFO
